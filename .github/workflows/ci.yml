name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm ci
      - name: Run type check
        run: npm run lint
      - name: Run basic smoke
        run: |
          node -v
          npm -v
      # Note: building native better-sqlite3 may require platform-specific tooling
